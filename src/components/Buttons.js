import React from "react";

export default function Buttons () {
    return (
        <div className="button-bar">
            <button type="submit" className="submit">ثبت</button>
            <button type="reset" className="reset">از اول</button>
        </div>
    )
}